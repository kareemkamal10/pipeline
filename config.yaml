# config.yaml — إعدادات الـ Datasets للجلسة الحالية
# تحديد أسماء الـ Datasets على Kaggle

# TTS Dataset: يحتوي على vocals + transcripts + metadata
dataset_tts_name: "history-lab-tts-v1"

# LLM Dataset: يحتوي على fulltranscripts فقط
dataset_llm_name: "history-lab-llm-v1"

# ملاحظات:
# - لو كان الاسم جديد: سيتم إنشاء dataset جديد
# - لو كان موجود: سيتم تحديث الـ dataset الموجود
# - احرص على استخدام أسماء مختلفة لكل جلسة أو حدّث الأسماء نفسها
